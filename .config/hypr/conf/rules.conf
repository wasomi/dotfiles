source = monitors.conf

windowrule {
    name = Utils
    match:class = ^(blueman-manager|org\.pulseaudio\.pavucontrol|network|systemUpdate|btop)$

    float = true
    size = 1280 720
    center = true
}

windowrule {
    name = Picture-in-Picture
    match:title = ^(Picture-in-Picture)$

    float = true
    size = 640 480
    move = (monitor_w*0.66) (monitor_h*0.05)
    pin = true
}

windowrule {
    name = Portal Dialogs
    match:class = ^(org\.freedesktop\.impl\.portal\.desktop\.(hyprland|gtk)|[Xx]dg-desktop-portal-gtk|hyprland-share-picker)$

    float = true
    center = true
}

windowrule {
    name = Dialogs
    match:title = ^(Choose Files|Save As|Confirm to replace files|File Operation Progress|Open|Authentication Required|Add Folder to Workspace|File Upload.*|.*dialog.*|Empty Trash|Rename.*|Save.*|Extract)$

    float = true
    center = true
    size = 1280 720
}

layerrule {
    name = Blur Rules
    match:namespace = ^(waybar|rofi|notifications)$

    blur = true
    blur_popups = true
    ignore_alpha = 0.5
}

windowrule {
    name = Pinned Satty
    match:class = com.gabm.satty

    float = true
    center = true
    pin = true
    size = 1600 900
}

windowrule {
    name = Games by Class
    match:class = ^(.*osu.*|steam_app_.*|steam)$

    immediate = true
    monitor = $mainMonitor
}

windowrule {
    name = Games by Title
    match:title = ^(Euro Truck Simulator 2)$

    immediate = true
    monitor = $mainMonitor
}

# Smart gaps
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = border_size 0, match:float 0, match:workspace w[tv1]
# windowrule = rounding 0, match:float 0, match:workspace w[tv1]
# windowrule = no_shadow true, match:float 0, match:workspace w[tv1]
# windowrule = border_size 0, match:float 0, match:workspace f[1]
# windowrule = rounding 0, match:float 0, match:workspace f[1]
# windowrule = no_shadow true, match:float 0, match:workspace f[1]
